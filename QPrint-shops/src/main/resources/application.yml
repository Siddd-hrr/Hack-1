server:
  port: 8088
spring:
  application:
    name: QPrint-shops
jwt:
  secret: ${JWT_SECRET:change-me-please-change-me-please-change-me}
  issuer: ${JWT_ISSUER:qprint-auth}
  audience: ${JWT_AUDIENCE:qprint-api}
management:
  endpoints:
    web:
      exposure:
        include: health,info
